<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kang.web.mapper.EntityMapper">

    <select id="queryPage" resultType="com.kang.web.entity.Entity">
        SELECT COUNT(*)
        FROM ${tableName}
        WHERE 1 = 1 ${whereSql};

    </select>
</mapper>
